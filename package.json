{
  "name": "filamanager-app",
  "version": "1.0.0",
  "description": "3D Filament Stock Manager with barcode scanning and Netlify Blobs storage",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site, no build step needed'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
